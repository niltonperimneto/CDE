lineToData_sources = ['lineToData.c']

lineToData = executable('lineToData',
  lineToData_sources,
  include_directories : include_directories('../TermPrim'),
  native : true # Always build for the build machine
)
